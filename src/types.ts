export interface SectionTitles {
  parallaxValue: string;
  value: string;
}
export interface ProjectProps {
  id: number;
  name: string;
  demoUrl: string;
  githubUrl: string;
  about: string;
  features: string[];
  technologies: string[];
  category: string;
  image: string;
}
